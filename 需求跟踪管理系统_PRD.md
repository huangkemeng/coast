# 需求跟踪管理系统产品需求文档（PRD）

---

## 版本记录

| 版本号 | 日期 | 修改人 | 修改摘要 |
|--------|------|--------|----------|
| v1.0 | 2025-05-06 | - | 初稿完成 |
| v1.1 | 2026-05-13 | - | 补充状态流转约束规则（3.3）、并发编辑控制策略（4.1.3.1）；新增需求 version 字段 |
| v1.4 | 2026-05-13 | - | 修正 3.3.1 状态流转规则：允许非终态间逆向流转至相邻状态；已上线仍为终态不可逆转 |
| v1.3 | 2026-05-13 | - | 新增用户认证模块（4.6节）：用户注册、登录、首次登录改密、密码找回、认证机制、安全策略、初始管理员账号 |
| v1.2 | 2026-05-13 | - | 修复流程图重复节点（ISS-08）；补充 URL 校验规则（ISS-03）、需求已确认自动控制（ISS-04）、通知重试策略（ISS-05）、用户/机器人删除级联（ISS-07）、报价精度与权限（ISS-10/ISS-12）、分页默认配置（ISS-11）；更新开放问题状态 |

---

## 1. 背景与目标

### 1.1 业务痛点
- 用户提出的需求数量多，产品经理/需求管理员主动跟踪困难，容易出现遗漏
- 需求状态变更信息无法及时同步给相关人员
- 缺乏统一的需求管理视图，无法快速了解需求进度和负责人

### 1.2 期望价值
- 通过企业微信机器人实现**被动式管理**，状态变更自动通知，减少人工跟进成本
- 建立需求-人员-项目的关联关系，责任清晰
- 通过时间提醒功能，提前预警即将到期的需求

### 1.3 成功指标
| 指标 | 目标值 |
|------|--------|
| 需求跟踪覆盖率 | 100% |
| 状态变更通知及时率 | 100%（5分钟内送达）|
| 到期提醒准确率 | 100% |
| 用户满意度 | ≥4.0/5.0 |

---

## 2. 用户角色

| 角色 | 核心诉求 | 主要权限 |
|------|----------|----------|
| **需求管理员/产品经理** | 全面掌控需求进度，及时发现问题 | 需求的增删改查、状态变更、人员分配、机器人配置、项目配置、报价查看与编辑 |
| **开发人员** | 了解自己负责的需求，及时获取状态变更 | 查看分配给自己的需求、更新需求进度、接收通知。报价字段不可见 |
| **测试人员** | 跟踪待测试需求，了解上线计划 | 查看分配给自己的需求、更新测试状态、接收通知。报价字段不可见 |

---

## 3. 核心业务流程

### 3.1 需求状态流转图

```
┌─────────┐   确认需求    ┌─────────┐   发起报价    ┌─────────┐
│  待确认  │ ───────────→ │  已确认  │ ───────────→ │ 待报价  │
└─────────┘              └─────────┘              └─────────┘
                                                       │
                                                 填写报价信息
                                                       ↓
┌─────────┐   确认报价    ┌─────────┐
│ 待开发   │ ←─────────── │ 已报价  │
└─────────┘              └─────────┘
     │
     ↓ 分配开发
┌─────────┐   开发完成    ┌─────────┐   测试完成    ┌───────────┐
│ 开发中   │ ───────────→ │ 测试中   │ ───────────→ │ 已验收     │
└─────────┘              └─────────┘              │ 待上线     │
                                                  └───────────┘
                                                         │
                                                 确认上线完成
                                                         ↓
                                                  ┌───────────┐
                                                  │  已上线    │
                                                  └───────────┘
```

### 3.2 状态流转规则

| 当前状态 | 可流转至 | 触发条件 |
|----------|----------|----------|
| 待确认 | 已确认 | 需求管理员确认需求内容 |
| 已确认 | 待报价 | 需求管理员发起报价流程 |
| 待报价 | 已报价 | 需求管理员填写报价信息 |
| 已报价 | 待开发 | 需求管理员确认报价并分配开发 |
| 待开发 | 开发中 | 开发人员开始处理需求 |
| 开发中 | 测试中 | 开发人员完成开发并提交测试 |
| 测试中 | 已验收待上线 | 测试人员完成测试并通过验收 |
| 已验收待上线 | 已上线 | 需求管理员确认上线完成 |

### 3.3 状态流转约束规则

#### 3.3.1 合法流转路径

系统采用**线性流转**模型，需求状态必须按 3.2 定义的路径依次流转，**不允许跨状态跳转**。

| 场景 | 示例 | 是否允许 |
|------|------|----------|
| 正向线性流转 | 待确认 → 已确认 → 待报价 → 已报价 → 待开发 → 开发中 → 测试中 → 已验收待上线 → 已上线 | ✅ 允许 |
| 跨状态跳转 | 待确认 → 开发中（跳过已确认/待报价/已报价/待开发） | ❌ 不允许 |
| 跨状态跳转 | 已确认 → 已上线（跳过中间所有状态） | ❌ 不允许 |
| 逆向流转（非终态间） | 开发中 → 待开发（回退到上一个状态） | ✅ 允许 |
| 逆向流转（终态） | 已上线 → 任意状态（回退或跳转） | ❌ 不允许 |

**实现规则**：
- 系统在保存需求时校验状态变更是否合法，仅允许从当前状态变更为其前后相邻状态（即 3.2 表中"当前状态"的上一行或下一行对应状态）。**不允许跨相邻状态跳转**
- 前端下拉框显示当前状态的前后合法相邻状态（而非全部9个状态），后端同样进行二次校验
- "已上线"为终态，不可逆向流转，见 3.3.2

#### 3.3.2 "已上线"终态规则

- **"已上线"为终态**，需求一旦变更为"已上线"，不再允许任何状态变更
- 已上线需求仅可进行信息查看和备注补充，**不可编辑状态字段**
- 若已上线需求因线上缺陷或其他原因需要重新进入开发流程，应**新建一条需求**记录，在原需求备注中标注关联新需求号，而非回退或修改原需求状态

---

## 4. 功能需求

### 4.1 需求管理模块

#### 4.1.1 需求列表查看

| 属性 | 说明 |
|------|------|
| **功能描述** | 展示所有需求的列表视图，支持筛选、排序、分页 |
| **前置条件** | 用户已登录系统 |
| **输入/前置状态** | 无 |
| **正常流程** | 1. 用户进入需求列表页面<br>2. 系统展示需求列表，默认按创建时间倒序<br>3. 用户可通过筛选条件过滤需求<br>4. 用户可点击表头进行排序 |
| **筛选条件** | 需求状态、跟进人、所属项目、计划时间范围 |
| **列表字段** | 需求名称、需求号、进度、跟进人、计划开始时间、计划交测时间、计划上线时间、实际交测时间、实际上线时间、需求已确认、需求文档链接、报价、所属系统、当前状态 |
| **分页配置** | 默认每页 20 条，支持切换每页 10/20/50 条 |
| **异常流程** | 数据为空时显示"暂无数据"提示 |
| **后置条件/输出** | 展示符合条件的需求列表 |
| **验收标准** | Given 用户已登录，When 进入需求列表页，Then 正确展示需求列表并支持筛选排序 |

#### 4.1.2 需求创建

| 属性 | 说明 |
|------|------|
| **功能描述** | 创建新的需求记录 |
| **前置条件** | 用户已登录，拥有需求创建权限（需求管理员） |
| **输入/前置状态** | 无 |
| **表单字段** | 详见下方需求字段定义 |
| **正常流程** | 1. 用户点击"新建需求"按钮<br>2. 系统弹出创建表单<br>3. 用户填写需求信息<br>4. 用户点击保存<br>5. 系统校验数据并保存<br>6. 发送企业微信通知（如配置了机器人） |
| **异常流程** | - 必填项未填写：提示"请填写XXX"<br>- 需求号重复：提示"需求号已存在"<br>- 时间逻辑错误（计划交测时间早于计划开始时间）：提示"时间设置不合理" |
| **后置条件/输出** | 需求创建成功，状态为"待确认"，发送通知 |
| **验收标准** | Given 用户有创建权限，When 填写完整信息并保存，Then 需求创建成功并发送通知 |

#### 4.1.3 需求编辑

| 属性 | 说明 |
|------|------|
| **功能描述** | 修改已有需求的详细信息 |
| **前置条件** | 用户已登录，拥有需求编辑权限（需求管理员或跟进人） |
| **正常流程** | 1. 用户在列表点击"编辑"<br>2. 系统加载需求详情<br>3. 用户修改字段<br>4. 用户保存<br>5. 系统检测状态变更，如发生变更则发送通知 |
| **异常流程** | 同需求创建 |
| **后置条件/输出** | 需求更新成功，状态变更时发送通知 |
| **验收标准** | Given 用户有编辑权限，When 修改需求信息，Then 更新成功，状态变更时触发通知 |

#### 4.1.3.1 并发编辑控制

| 属性 | 说明 |
|------|------|
| **策略名称** | 乐观锁（版本号）机制 |
| **版本号存储** | 每条需求记录维护一个整数版本号字段 `version`，初始值为 1，每次成功更新后自增 1 |
| **正常流程** | 1. 用户A打开需求编辑页，系统返回当前数据及版本号（如 v3）<br>2. 用户B也打开同一需求编辑页，同样获取版本号 v3<br>3. 用户B先提交保存，系统校验版本号 v3 = 数据库当前 v3，校验通过，数据更新，版本号变为 v4<br>4. 用户A后提交保存，系统校验版本号 v3 ≠ 数据库当前 v4，校验失败<br>5. 系统拒绝用户A的保存请求，返回提示 |
| **冲突提示** | "数据已被他人修改，请刷新页面获取最新数据后重新编辑" |
| **异常流程** | 用户收到冲突提示后，需手动刷新页面重新加载最新数据，再次编辑后提交 |
| **后置条件** | 仅第一个保存的用户更新成功；后续用户需基于最新版本重新编辑；每次成功更新版本号自增 |

#### 4.1.4 需求删除

| 属性 | 说明 |
|------|------|
| **功能描述** | 删除需求记录 |
| **前置条件** | 用户已登录，拥有需求删除权限（仅需求管理员） |
| **正常流程** | 1. 用户点击删除按钮<br>2. 系统弹出确认对话框<br>3. 用户确认删除<br>4. 系统删除需求 |
| **异常流程** | 用户取消删除操作 |
| **后置条件/输出** | 需求删除成功 |
| **验收标准** | Given 用户有删除权限，When 确认删除需求，Then 需求从系统中移除 |

#### 4.1.5 需求字段定义

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 需求名称 | 文本 | 是 | 需求的简要描述，最多100字 |
| 需求号 | 文本 | 是 | 支持手动输入，唯一标识，最多50字 |
| 当前状态 | 枚举 | 是 | 待确认/已确认/待报价/已报价/待开发/开发中/测试中/已验收待上线/已上线 |
| 进度 | 数值 | 否 | 0-100的整数，表示完成百分比 |
| 跟进人 | 关联用户 | 是 | 从系统用户中选择 |
| 计划开始时间 | 日期 | 否 | 预计开始开发的日期 |
| 计划交测时间 | 日期 | 否 | 预计提交测试的日期 |
| 计划上线时间 | 日期 | 否 | 预计上线的日期 |
| 实际交测时间 | 日期 | 否 | 实际提交测试的日期，状态变为"测试中"时自动填充 |
| 实际上线时间 | 日期 | 否 | 实际上线的日期，状态变为"已上线"时自动填充 |
| 需求已确认 | 布尔 | 否 | 由系统自动控制，不可手动编辑。状态变为"已确认"及以上时自动设为"是"，回退为"待确认"时自动设为"否" |
| 需求文档链接 | URL | 否 | 需求文档的链接地址。校验规则：必须是有效的 http:// 或 https:// 开头的完整URL，不支持内网地址（如 localhost、192.168.x.x） |
| 报价 | 数值 | 否 | 需求报价金额，单位元。精度：支持最多2位小数，范围 ≥0。仅需求管理员可见和编辑 |
| 所属系统/项目 | 关联项目 | 是 | 从项目列表中选择 |
| 通知机器人 | 关联机器人 | 否 | 选择要接收通知的企业微信机器人 |
| 优先级 | 枚举 | 否 | 高/中/低，默认中，影响提醒时间 |
| 备注 | 文本 | 否 | 补充说明，最多500字 |
| 创建时间 | 日期时间 | 系统自动 | 需求创建时间 |
| 更新时间 | 日期时间 | 系统自动 | 最后更新时间 |
| 版本号 | 整数 | 系统自动 | 并发控制版本号，初始为1，每次成功更新后自增1 |

---

### 4.2 项目管理模块

#### 4.2.1 项目列表管理

| 属性 | 说明 |
|------|------|
| **功能描述** | 管理项目/系统信息，供需求关联使用 |
| **前置条件** | 用户已登录，拥有项目管理权限 |
| **正常流程** | 1. 进入项目管理页面<br>2. 查看项目列表<br>3. 可进行增删改查操作 |
| **项目字段** | 项目名称（必填）、项目编码、项目负责人、项目描述、创建时间 |
| **验收标准** | Given 用户有权限，When 操作项目数据，Then 项目信息正确维护 |

#### 4.2.2 项目与需求关系

- 一个需求**只能关联一个项目**
- 删除项目时，如该项目下存在需求，提示"该项目下存在需求，无法删除"

---

### 4.3 企业微信机器人配置模块

#### 4.3.1 机器人管理

| 属性 | 说明 |
|------|------|
| **功能描述** | 配置企业微信机器人，供需求通知使用 |
| **前置条件** | 用户已登录，拥有机器人配置权限（需求管理员） |
| **正常流程** | 1. 进入机器人配置页面<br>2. 点击"添加机器人"<br>3. 填写机器人信息<br>4. 测试机器人连接<br>5. 保存配置 |
| **机器人字段** | 机器人名称（必填）、Webhook地址（必填）、所属群组、启用状态、创建时间 |
| **异常流程** | Webhook地址无效：测试连接失败，提示"Webhook地址无效，请检查" |
| **验收标准** | Given 填写有效Webhook，When 测试并保存，Then 机器人配置成功并可发送消息 |

#### 4.3.2 机器人与需求关联

- 需求创建/编辑时可选择通知机器人（下拉选择，可选）
- 一个需求**只能关联一个机器人**
- 一个机器人可被**多个需求关联**
- 删除机器人时，系统自动将关联该机器人的所有需求的「通知机器人」字段置空，并在通知日志中记录"机器人已删除，关联清除"

---

### 4.4 通知管理模块

#### 4.4.1 状态变更通知

| 属性 | 说明 |
|------|------|
| **功能描述** | 需求状态发生变更时，自动发送企业微信通知 |
| **触发条件** | 需求状态字段发生变化 |
| **通知内容** | 需求名称、需求号、原状态、新状态、变更时间、跟进人、相关时间信息 |
| **通知对象** | 需求关联的机器人（发送到对应群组） |
| **通知模板示例** | ```<br>【需求状态变更】<br>需求名称：XXX功能开发<br>需求号：REQ-001<br>状态变更：开发中 → 测试中<br>跟进人：张三<br>计划交测时间：2025-05-10<br>实际交测时间：2025-05-08<br>备注：提前2天完成开发<br>``` |
| **验收标准** | Given 需求状态发生变更，When 保存成功后，Then 5分钟内发送企业微信通知 |

#### 4.4.1.1 通知发送失败重试策略

| 属性 | 说明 |
|------|------|
| **重试机制** | 通知发送失败后自动重试 |
| **重试次数** | 最多重试 3 次 |
| **重试间隔** | 第1次重试间隔 30 秒，第2次间隔 2 分钟，第3次间隔 5 分钟（指数递增） |
| **全部失败处理** | 3 次重试均失败后，记录通知日志（发送状态=失败，失败原因明确记录），不再重试 |
| **后续补偿** | 每日上午 9:00 定时任务扫描过去 24 小时内发送失败的通知，重新尝试发送 1 次 |

#### 4.4.2 时间提醒通知

| 属性 | 说明 |
|------|------|
| **功能描述** | 根据需求优先级，在计划时间到期前发送提醒 |
| **提醒规则** | 根据优先级差异化提醒： |
| **高优先级** | 计划交测/上线前3天、1天、当天各提醒一次 |
| **中优先级** | 计划交测/上线前2天、当天各提醒一次 |
| **低优先级** | 计划交测/上线前1天提醒一次 |
| **提醒时间** | 每天上午9:00统一扫描并发送提醒 |
| **提醒内容** | 需求名称、需求号、到期类型（交测/上线）、计划时间、剩余天数、跟进人 |
| **验收标准** | Given 需求设置了计划时间，When 到达提醒时间点，Then 发送提醒通知 |

#### 4.4.3 通知日志

| 属性 | 说明 |
|------|------|
| **功能描述** | 记录所有发送的通知，便于排查问题 |
| **记录字段** | 通知时间、通知类型（状态变更/时间提醒）、需求号、接收机器人、发送状态（成功/失败）、失败原因 |
| **验收标准** | Given 发送通知，When 无论成功失败，Then 记录通知日志 |

---

### 4.5 用户管理模块

#### 4.5.1 用户列表

| 属性 | 说明 |
|------|------|
| **功能描述** | 管理系统用户，供需求跟进人选择 |
| **用户字段** | 用户名、姓名、角色、手机号、邮箱、启用状态、创建时间 |
| **角色定义** | 管理员（全部权限）、开发人员（查看和更新分配给自己的需求）、测试人员（查看和更新分配给自己的需求） |
| **验收标准** | Given 管理员操作，When 增删改查用户，Then 用户信息正确维护 |

#### 4.5.1.1 用户删除约束

- 用户被删除前，系统检查该用户是否为任意需求的「跟进人」
- 若存在关联需求，提示"该用户是 X 条需求的跟进人，无法删除。请先变更这些需求的跟进人后再删除用户"，阻止删除操作
- 被禁用用户仍出现在现有需求的跟进人字段中，但新建/编辑需求时下拉列表不显示已禁用用户

---

### 4.6 用户认证模块

#### 4.6.1 用户注册

| 属性 | 说明 |
|------|------|
| **功能描述** | 新用户注册系统账号（支持自主注册 + 管理员创建） |
| **前置条件** | 无 |
| **注册方式** | ① 用户自主注册 ② 管理员创建账号 |
| **表单字段** | 用户名（必填，唯一）、密码（必填）、确认密码（必填，与密码一致）、姓名（必填）、手机号（必填，唯一）、邮箱（可选） |
| **密码规则** | 至少8位，必须包含大小写字母和数字 |
| **正常流程（自主注册）** | 1. 用户点击"注册"<br>2. 填写注册信息<br>3. 点击"注册"提交<br>4. 系统校验数据<br>5. 密码加密存储<br>6. 注册成功，账号状态为"待启用"<br>7. 跳转到登录页，提示"注册成功，请等待管理员启用账号" |
| **正常流程（管理员创建）** | 1. 管理员进入用户管理页面<br>2. 点击"添加用户"<br>3. 填写用户信息（用户名、姓名、手机号、角色）<br>4. 系统生成随机初始密码（如 `User@123456`）<br>5. 创建成功，弹窗显示初始密码<br>6. 管理员手动将密码告知用户 |
| **异常流程** | - 用户名重复：提示"用户名已存在"<br>- 手机号重复：提示"该手机号已注册"<br>- 密码不符合规则：提示"密码至少8位，必须包含大小写字母和数字"<br>- 两次密码不一致：提示"两次输入密码不一致" |
| **后置条件** | 注册成功/创建成功，账号状态为"待启用"，等待管理员启用 |
| **验收标准** | Given 填写完整符合规则的信息，When 点击注册/创建，Then 账号创建成功，状态为待启用 |

#### 4.6.2 用户登录

| 属性 | 说明 |
|------|------|
| **功能描述** | 用户登录系统 |
| **前置条件** | 账号已注册且状态为已启用 |
| **登录方式** | 用户名 + 密码 |
| **表单字段** | 用户名（必填）、密码（必填）、记住登录（可选，7天内免登录） |
| **正常流程** | 1. 输入用户名和密码<br>2. 点击登录<br>3. 系统验证账号密码<br>4. 验证通过，生成认证Token<br>5. 判断是否为首次登录（首次登录标记=true）<br>6. 若是首次登录，跳转到修改密码页<br>7. 若非首次登录，跳转到首页/需求列表 |
| **异常流程** | - 账号不存在：提示"账号不存在，请检查输入"<br>- 密码错误：提示"密码错误，剩余N次机会"<br>- 账号待启用：提示"账号尚未启用，请联系管理员"<br>- 账号已禁用：提示"账号已被禁用，请联系管理员"<br>- 连续错误5次：账号锁定30分钟 |
| **后置条件** | 登录成功，生成会话Token |
| **验收标准** | Given 输入正确用户名密码，When 点击登录，Then 登录成功，首次登录跳转修改密码页，非首次登录跳转首页 |

#### 4.6.3 首次登录强制修改密码

| 属性 | 说明 |
|------|------|
| **功能描述** | 首次登录时强制修改初始密码 |
| **触发条件** | 用户首次登录（首次登录标记=true） |
| **前置条件** | 用户已完成登录认证 |
| **页面限制** | 若未修改密码，禁止访问其他页面 |
| **表单字段** | 当前密码（初始密码）、新密码（必填）、确认新密码（必填，与新密码一致） |
| **新密码规则** | 至少8位，必须包含大小写字母和数字，不能与当前密码相同 |
| **正常流程** | 1. 系统展示修改密码页面<br>2. 用户输入当前密码和新密码<br>3. 点击确认<br>4. 系统验证当前密码正确性<br>5. 验证新密码符合规则<br>6. 更新密码，清除"首次登录"标记<br>7. 跳转到首页 |
| **异常流程** | - 当前密码错误：提示"当前密码错误"<br>- 新密码不符合规则：提示"新密码至少8位，必须包含大小写字母和数字"<br>- 新密码与当前密码相同：提示"新密码不能与当前密码相同" |
| **验收标准** | Given 首次登录，When 输入正确当前密码和新密码并确认，Then 密码修改成功，跳转到首页 |

#### 4.6.4 密码重置/找回

| 属性 | 说明 |
|------|------|
| **功能描述** | 用户忘记密码时重置密码 |
| **触发方式** | 登录页"忘记密码"入口 |
| **校验方式** | 手机号 + 短信验证码 |
| **正常流程** | 1. 用户点击"忘记密码"<br>2. 输入注册手机号<br>3. 点击"获取验证码"<br>4. 系统发送6位数字验证码到手机（有效期5分钟）<br>5. 用户输入验证码<br>6. 系统验证验证码正确性<br>7. 用户输入新密码和确认密码<br>8. 点击确认<br>9. 密码更新成功<br>10. 跳转登录页 |
| **验证码规则** | 6位数字，有效期5分钟，每个手机号每分钟最多发送1次，每天最多发送10次 |
| **异常流程** | - 手机号未注册：提示"该手机号未注册"<br>- 验证码错误：提示"验证码错误，请重新获取"<br>- 验证码过期：提示"验证码已过期，请重新获取"<br>- 验证码次数超限：提示"验证码获取次数超限，请明天再试" |
| **验收标准** | Given 通过手机验证码验证，When 重置密码成功，Then 可使用新密码登录 |

#### 4.6.5 修改密码（已登录用户）

| 属性 | 说明 |
|------|------|
| **功能描述** | 已登录用户修改自己的密码 |
| **入口** | 个人设置/账户安全页面 |
| **表单字段** | 当前密码（必填）、新密码（必填）、确认新密码（必填） |
| **新密码规则** | 同4.6.1 |
| **正常流程** | 1. 输入当前密码<br>2. 输入新密码和确认密码<br>3. 点击保存<br>4. 验证当前密码<br>5. 更新密码<br>6. 提示"密码修改成功" |
| **异常流程** | 同4.6.3 |
| **验收标准** | Given 已登录用户，When 输入正确当前密码和新密码，Then 密码修改成功 |

#### 4.6.6 认证与会话管理

| 属性 | 说明 |
|------|------|
| **认证机制** | JWT（JSON Web Token）无状态认证 |
| **Token结构** | Access Token（访问令牌）+ Refresh Token（刷新令牌） |
| **Access Token** | 有效期2小时，用于API接口认证 |
| **Refresh Token** | 有效期7天，用于获取新的Access Token |
| **Token续期** | Access Token过期前，客户端使用Refresh Token自动获取新Token |
| **退出登录** | 清除本地Token，后端将Refresh Token加入黑名单 |
| **记住登录** | 勾选后Refresh Token有效期延长至30天 |
| **多设备登录** | 不限制同时登录设备数 |

#### 4.6.7 登录安全机制

| 安全措施 | 规则 | 说明 |
|----------|------|------|
| **密码加密** | bcrypt算法，强度12 | 密码加密存储，不可逆 |
| **密码错误限制** | 连续错误5次 | 锁定账号30分钟 |
| **登录锁定** | 锁定期间禁止登录 | 解锁后错误计数清零 |
| **验证码限制** | 每手机号每分钟1次 | 防止短信轰炸 |
| **登录日志** | 记录所有登录尝试 | 包括成功/失败的时间、IP地址、User-Agent |
| **敏感操作日志** | 记录密码修改、权限变更 | 用于安全审计 |

#### 4.6.8 初始管理员账号

| 属性 | 说明 |
|------|------|
| **创建方式** | 系统首次启动时自动创建默认管理员 |
| **默认账号** | 用户名: `admin` |
| **默认密码** | 固定默认密码 `Admin@123` |
| **首次登录** | 首次登录时强制修改密码（同4.6.3） |
| **账号保护** | 默认管理员账号不可删除，不可禁用 |
| **验收标准** | Given 系统首次部署，When 访问系统，Then 可使用初始管理员账号登录 |

#### 4.6.9 登录页面设计

| 元素 | 说明 |
|------|------|
| **页面标题** | 需求跟踪管理系统 |
| **Logo** | 系统Logo（可配置） |
| **表单** | 用户名输入框、密码输入框、记住登录复选框 |
| **按钮** | 登录按钮 |
| **链接** | "忘记密码？"链接 |
| **注册入口** | "还没有账号？立即注册"链接（需确认是否显示） |
| **版权信息** | 系统版本号、版权所有 |
| **页面样式** | 简洁大方，适配PC和移动端 |

#### 4.6.10 注册页面设计

| 元素 | 说明 |
|------|------|
| **表单字段** | 用户名、密码、确认密码、姓名、手机号、邮箱（可选） |
| **按钮** | 注册按钮 |
| **链接** | "已有账号？立即登录"链接 |
| **密码强度提示** | 实时显示密码强度（弱/中/强） |
| **表单验证** | 实时校验输入格式，提交前完整校验 |

---

## 5. 非功能需求

| 类别 | 需求描述 | 量化指标 |
|------|----------|----------|
| **性能** | 需求列表查询响应时间 | 95%请求在2秒内响应 |
| **性能** | 通知发送延迟 | 状态变更后5分钟内送达 |
| **可用性** | 系统可用性 | 99.5%（月度） |
| **安全** | 用户认证 | 所有接口需登录认证，敏感操作需管理员权限 |
| **安全** | 数据访问控制 | 用户只能查看/操作有权限的数据 |
| **兼容性** | 浏览器支持 | Chrome、Edge、Firefox最新两个主版本 |
| **扩展性** | 数据量支持 | 支持10万条需求记录，性能不下降 |
| **可维护性** | 日志记录 | 关键操作和异常均需记录日志 |

---

## 6. 约束与依赖

### 6.1 技术约束
- 企业微信机器人通知依赖企业微信开放平台的Webhook接口
- [待澄清] 是否需要支持其他通知渠道（如钉钉、飞书）？

### 6.2 外部依赖
| 依赖项 | 说明 |
|--------|------|
| 企业微信 | 机器人消息推送依赖企业微信Webhook |
| 数据库 | 需求、项目、用户、通知日志等数据存储 |

### 6.3 数据迁移
- [待澄清] 是否有历史需求数据需要迁移？

### 6.4 功能依赖关系
```
用户认证 → 用户管理（认证后才有用户管理）
用户认证 → 需求管理（所有需求操作需先登录）
用户认证 → 项目管理（项目管理需先登录）
用户认证 → 机器人配置（机器人配置需先登录）
用户管理 → 需求管理（跟进人选择依赖用户数据）
项目管理 → 需求管理（所属项目选择依赖项目数据）
机器人配置 → 需求管理（通知机器人选择依赖机器人配置）
需求管理 → 通知管理（状态变更触发通知）
```

---

## 7. 优先级划分

### 7.1 Must-have（MVP，必须实现）
| 功能 | 说明 |
|------|------|
| 用户认证 | 用户注册、登录、首次登录改密、密码找回、退出登录 |
| 需求CRUD | 需求的增删改查 |
| 需求字段 | 所有核心字段（除机器人关联外） |
| 状态流转 | 9个状态的正常流转 |
| 项目管理 | 项目的基础增删改查 |
| 用户管理 | 用户的基础增删改查 |
| 机器人配置 | Webhook配置和测试 |
| 状态变更通知 | 状态变更时发送企业微信通知 |

### 7.2 Should-have（应该实现）
| 功能 | 说明 |
|------|------|
| 时间提醒通知 | 按优先级差异化提醒 |
| 通知日志 | 通知发送记录查询 |
| 需求筛选排序 | 列表的高级筛选功能 |
| 数据导出 | 需求列表导出Excel |

### 7.3 Could-have（可以实现）
| 功能 | 说明 |
|------|------|
| 数据统计报表 | 需求完成率、延期率统计 |
| 批量操作 | 批量修改跟进人、批量修改状态 |
| 需求模板 | 常用需求字段模板 |

### 7.4 Won't-have（暂不实现）
| 功能 | 说明 |
|------|------|
| 移动端APP | 本期仅支持Web端 |
| 与Jira/禅道集成 | 独立系统，暂不与外部系统对接 |
| 邮件通知 | 仅企业微信通知 |

---

## 8. 开放问题

| 编号 | 问题 | 状态 | 方案/说明 |
|------|------|------|----------|
| 1 | 是否支持其他通知渠道（钉钉、飞书）？ | 待确认 | 建议首期仅支持企业微信，后续迭代考虑扩展 |
| 2 | 是否有历史需求数据需要迁移？ | 待确认 | 如需迁移，需确认数据格式和迁移范围 |
| 3 | 用户登录方式？ | ✅ 已决策 | 见v1.3：支持用户名+密码登录，首次登录强制修改密码 |
| 4 | 是否需要需求评审流程？ | 待确认 | 建议首期简化流程，后续根据实际业务需要增加评审节点 |
| 5 | 报价字段是否涉及敏感信息？ | ✅ 已决策 | 见 v1.2：报价字段仅需求管理员可见和编辑，开发人员和测试人员不可见 |
| 6 | 初始管理员默认密码设置方式？ | ✅ 已决策 | 固定默认密码 `Admin@123`，首次登录强制修改 |
| 7 | 同一账号同时登录设备数限制？ | ✅ 已决策 | 不限制同时登录设备数 |
| 8 | 管理员创建用户后如何通知密码？ | ✅ 已决策 | 仅页面显示：创建成功后弹窗显示初始密码，管理员需手动告知用户 |

---

## 需求检查清单

- [x] 每个功能是否都有明确的验收标准？
- [x] 异常流程和边界情况是否覆盖？
- [x] 是否定义了用户权限与数据权限？
- [x] 非功能需求是否量化（如"95%请求在2秒内响应"）？
- [x] 功能依赖关系是否清晰标注？
- [x] 是否存在模糊或歧义的描述？
- [x] MVP范围是否明确界定？
- [x] 用户认证模块是否完整（注册/登录/改密/找回）？

---

**文档结束**

是否需要针对某个功能点进一步细化？或补充数据字段定义？
